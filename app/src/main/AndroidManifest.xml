<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.team" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyTheme" >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name=".Ranking"/>

        <activity android:label="@string/app_name" android:name=".Setting"/>

        <activity android:label="@string/app_name" android:name=".ShortTyping" android:windowSoftInputMode="stateAlwaysVisible|adjustNothing"/>

        <activity android:label="@string/app_name" android:name=".SpaceTyping" android:windowSoftInputMode="stateAlwaysVisible|adjustNothing"/>

        <activity android:label="@string/app_name" android:name=".TypingGame" android:windowSoftInputMode="stateAlwaysVisible|adjustNothing"/>

        <activity android:label="@string/app_name" android:name=".TypingMenu"/>

        <activity android:label="@string/app_name" android:name=".WordTyping" android:windowSoftInputMode="stateAlwaysVisible|adjustNothing"/>
    </application>

</manifest>
